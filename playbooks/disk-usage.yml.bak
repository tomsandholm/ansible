---
- name: Show /home usage
  hosts: all
  gather_facts: true
  become: yes
  tasks:
    - name: show /home usage
      ansible.builtin.debug: 
        var: item
      loop: "{{ ansible_facts['mounts'] }}"
